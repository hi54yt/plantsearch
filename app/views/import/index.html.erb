  <%= form_tag import_plants_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag "导入" %>
  <% end %>

  <%= link_to '删除所有数据',
                delete_path,
                :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                :class => 'btn btn-mini btn-danger' %>